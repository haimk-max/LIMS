# HANDOVER.md — LIMS

זיכרון בין-סשן. קרא בתחילת כל סשן; עדכן בסוף כל סשן (`/handover`).

## סשן 2026-08-11 — הקמת הריפו: מיגרציה מ-water-knowledge-system

- **מיגרציה מכנית, לא תוכן חדש**: כל תיקיית `docs/lims-icp/` (8 קבצים: `START_HERE_he.md`, קטלוג CSV, חוברת HTML v0.3, `tools/build_catalog_section.py`, ארבעה קבצי `sources/`) וכן `docs/LIMS_CAPABILITY_MAP.md` + `docs/LIMS_VENDOR_FINDINGS.md` הועברו מ-`water-knowledge-system` **ללא שינוי תוכן מהותי** — אומת בהשוואת md5sum/diff לפני מחיקה במקור. שני מסמכי-הממשל היחידים שנערכו: `START_HERE_he.md` (בית קנוני + מספור PROCESS, ראו §0-B שם) ו-CLAUDE/README/PROCESS/HANDOVER החדשים שנכתבו לריפו הזה מאפס.
- **תשתית `.claude/` הועתקה יזום, לא בדיעבד**: hook (`session-start.sh`), `settings.json` וסקיל `/handover` הועתקו כבר בסבב ההקמה — כדי לא לחזור על התקלה המתועדת ב-water-knowledge-system (שם נבנה הריפו ב-2026-07-09 בלי `/handover` זמין, ותוקן רק ב-2026-07-13 אחרי שסשן נתקל בזה).
- **PROCESS.md כאן מתחיל מ-#1** — SSOT עצמאי לריפו הזה. ההיסטוריה המלאה של בניית התוכן (כולל אימוץ הפרויקט המקביל מ-ChatGPT ב-2026-08-03) **נשארת** ב-`water-knowledge-system/PROCESS.md` #14–#16 — לא שוכפלה לכאן, רק מוזכרת בהפניה (CLAUDE.md §0).
- **מה נבדק לפני שהמקור נמחק**: (1) diff/md5sum על כל 10 קבצי-התוכן מול המקור — זהים; (2) לאחר push — `git ls-tree` מול ה-remote אימת שכל 16 הקבצים (10 תוכן + 4 ממשל + 3 `.claude/`... למעשה יש למנות בפועל, ראו PROCESS #1) קיימים בפועל בריפו המרוחק, לא רק ב-commit המקומי (לקח מפורש מ-water-knowledge-system/CLAUDE.md: "ב-2026-07-09 שני קובצי HTML הושמטו בשקט מ-push").
- **ענף**: הריפו נוצר ריק (רק README גרעיני) — הועלה ישירות ל-`main`, אין עדיין זרימת PR מבוססת. אם ברצונך לעבור לזרימת ענף+PR כמו בשני הריפואים האחים — לבקש במפורש.
- **הצעד הבא הסביר**: PROCESS #2 — סבב צוות-מעבדה על חוברת העבודה (6 חסמי-סף D-01…D-06 + 7 כללים מועמדים + נקודות A7–A9).
