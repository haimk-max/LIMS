# CLAUDE.md — LIMS (מסלול בחינת מערכת LIMS למעבדה)

> **מקור העקרונות הכלליים**: הועתק מ-`sessions-archive/templates/CLAUDE.base.md@a288687` (הבית הקנוני; אין סנכרון אוטומטי — עדכון מהותי שם מחייב סבב-הפצה יזום).

---

## עקרונות עבודה אדם–מודל (להעתקה)

### א. ממשל מסמכים ותיעוד
1. **עובדה אחת — בית אחד**: לכל עובדה (סטטוס, גרסה, החלטה, לקח) מקום קנוני יחיד; כל אזכור אחר הוא הפניה. שכפול עובדות — לא מספר הקבצים — הוא מקור הדריפט.
2. **מבחן קיום למסמך חדש**: מסמך מצדיק את קיומו רק אם הוא עונה על שאלה שאף מסמך קיים לא עונה עליה; אחרת — מיזוג.
3. **מקור אחד, פורמטים נגזרים**: תוצר בכמה פורמטים נכתב פעם אחת ומרונדר — אלא אם הפורמטים הם מסמכים מעוצבים בעבודת-יד עם תוכן עיצובי משלהם, שאז ריצת רינדור לא תבטל את העבודה הידנית ועדיף שומר-עקביות read-only.
4. **תיעוד גר ליד מושאו**: ממשל פר-פרויקט בריפו של הפרויקט; כל מה שחוצה-פרויקטים — בבית משותף אחד.
5. **טבלת תפקידי מסמכים**: קובץ הממשל מכיל טבלה קצרה "איזו שאלה כל מסמך עונה" — הפילטר למסמך הבא.

### ב. זיכרון ולמידה מתמשכת
6. **זיכרון בין-סשן committed**: הנחיות יציבות (CLAUDE.md) + מסירת-סשן נרטיבית (HANDOVER) + מעקב דרישות עם hash ושיטת אימות לכל סגירה (PROCESS). תוכניות בצ'אט נעלמות; קבצים נשארים.
7. **צינור קידום לקחים**: לקח מקודם לחוק כתוב רק אחרי מקרה שני שמאמת אותו.
8. **מטא-ביקורת תקופתית**: אחת לרבעון/אבן-דרך — בדיקת דריפט מסמכים, כלים לא-מסונכרנים, לקחים שהבשילו, ותוצרים בבית הלא-נכון.
9. **תעודת מעבר**: כל רעיון/תוצר שנודד בין כלים או פרויקטים מגיע עם בלוק FACT / ASSUMPTION / DECISION / OPEN — מה מחייב ומה רק השראה.
10. **רמת ודאות מוצהרת**: כל מסמך מסכם נפתח בהצהרת מקורות ומגבלות; טענות מהותיות מסומנות סטטוס אינליין.

### ג. עבודה עם מודלים
11. **תגובה מילולית לפני תכנון, תכנון לפני ביצוע**: ביקורת/משוב מקבלים קודם ניתוח ביקורתי מילולי; המודל מוזמן לחלוק ("לא מחויב למסקנות אך מחויב לשקול אותן").
12. **תיוג מוצא לכל קלט מודבק**: סקירה/משוב/טקסט נושא שורת מקור — אדם / מודל (איזה) / משולב.
13. **הקצאת מודלים לפי צפיפות-ערך**: מודל חזק לשיפוט; מודל זול לביצוע מובנה מבריף סגור.
14. **אימות מכני לעבודה מואצלת**: כל האצלה מלווה באימות שאינו תלוי בדיווח המבצע — SHA מול remote, צילום מסך, בדיקת מבנה. "בוצע בהצלחה" איננו ראיה.
15. **בדיקת השמטה ולא רק אימות**: בביקורת תוצרי מודל לחפש גם את מה שחסר, לא רק לאמת את הכתוב.
16. **שלב bootstrap מוצהר**: פרויקט שמומחה בונה לבד בסיוע AI מוגדר מראש כשלב עם נקודת-סיום שבה עובר לצוות/תשתית ארגונית.

### ד. חובת אובייקטיביות במחקר ובהערכת רעיונות
17. **השערה — לא הנחת-יסוד**: כשמתבקש מחקר או הערכה של רעיון (במיוחד רעיון/אבחנה של המבקש עצמו) — ההשערה נכנסת למחקר כהשערה לבדיקה מול הסברים מתחרים. אסור להטמיע אותה כהנחת-יסוד בבריפים לסוכני מחקר; בריף מוטה = מחקר מוטה, גם אם הממצאים נכונים בפרטיהם.
18. **חיפוש ראיות-נגד מובנה**: כל מחקר כולל ציר ייעודי לראיות סותרות ולהסברים חלופיים; טענות מכריעות-החלטה עוברות סבב הפרכה אדברסרי (סוכנים שמנדטם להפריך) לפני הצגת מסקנות.
19. **היגיינת ראיות**: בדיקת אינטרס-מקור (ספק שמוכר את הפתרון = חשד); בדיקת "מה קרה אחר-כך" לכל סיפור-הצלחה מצוטט; הבחנה בין מדדי-פרוקסי (השתתפות, תפוקה) לתוצאות בפועל (מימוש, אימוץ).
20. **חשד מוגבר להתאמה נוחה**: ממצא שמתיישב עם עמדה קודמת של המבקש מקבל בדיקה מחמירה יותר, לא מקלה; הדוח מציין במפורש היכן מסקנותיו חופפות לעמדות המבקש ועל איזה בסיס עצמאי הן עומדות. (מבחן-העזר: "אילו המבקש האמין בהיפך — האם המחקר הזה היה מגלה זאת?")

הפרוטוקול המלא למחקר: `sessions-archive/toolkit/process/playbooks/objective-research.md`.

---

## אילוצי סביבה

- המשתמש עובד **אך ורק** דרך Claude Code on the web — **אין גישה לטרמינל**.
- `~/.claude/` לא שורד בין סשנים. כל זיכרון בין-סשן חייב להיות committed לריפו.
- **זיכרון בין-סשן**: `HANDOVER.md` בשורש — קרא בתחילת כל סשן; סקיל `/handover` בסוף כל סשן.
- **מעקב דרישות**: `PROCESS.md` — SSOT לדרישות פתוחות/סגורות (hash + שיטת אימות לכל סגירה). SSOT **של ריפו זה בלבד** — לא המשך המספור מ-water-knowledge-system (ראו §0 בהמשך).

## קונבנציות שפה

- **קבצי Markdown** (ממשל, תוכניות, דוחות): **עברית**. **קוד** (Python/JS, שמות משתנים, comments, docstrings): **אנגלית**. **Commit messages**: אנגלית.
- **HTML עברי**: RTL מלא, `<bdi>` סביב טוקנים לטיניים, חצים ← בזרימת טקסט עברי (לא →). חוברת העבודה (`docs/lims-icp/ICP_RULE_ENGINE_WORKBOOK_he.html`) כבר בנויה לפי הכלל הזה — תבנית קנונית: `sessions-archive/toolkit/process/templates/hebrew-doc.html`; QA חזותי: `toolkit/process/playbooks/screenshot-qa.md`.

## תזכורת toolkit (עדכון הבית הקנוני חוצה-הפרויקטים)

שינית/הוספת **כלי** שעשוי לשמש פרויקט אחר — כמו `tools/build_catalog_section.py` (מחולל קטע-קטלוג מ-CSV; דפוס שעשוי להתאים למסמכי-כללים אחרים)? עדכן את הבית הקנוני ב-`sessions-archive/toolkit/` **באותו סבב** ורענן את הערת ה-provenance בעותק המקומי. **אין סנכרון אוטומטי** — עותק הוא fork שמסטה. תהליכי ← `toolkit/process/`; דומייני ← `toolkit/domain/`. הבדיקה התקופתית — במטא-ביקורת.

## קשרי פרויקטים

| פרויקט | יחס |
|---|---|
| `water-knowledge-system` | **פרויקט-מקור**: תיקיית `docs/lims-icp/` נבנתה ופותחה שם עד 2026-08-11 (ראו §0 — תעודת מעבר); אין תלות קוד. מפל תנאי-הסף של הצעת מערכת הידע שם אינו נוגע לריפו זה, ולהפך |
| `sessions-archive` | הבית הקנוני לכלים תהליכיים, תבניות ו-KB |
| `PFAS-Forensics` | פרויקט-אח נוסף באותה משפחה; אין תלות קוד |

---

## §0. תעודת מעבר — מיגרציה מ-water-knowledge-system (2026-08-11)

**בית קנוני קודם**: `water-knowledge-system`, תיקיית `docs/lims-icp/` (וכן `docs/LIMS_CAPABILITY_MAP.md`, `docs/LIMS_VENDOR_FINDINGS.md`), מ-2026-08-02 עד 2026-08-11. הריפו הנוכחי הוא הבית הקנוני **החל מ-2026-08-11** — לא water-knowledge-system.

- **FACT**: כל תוכן `docs/lims-icp/` (כולל `sources/`, `tools/build_catalog_section.py`, קטלוג ה-CSV וחוברת העבודה v0.3) וכן שני מסמכי הרקע הכלליים (`LIMS_CAPABILITY_MAP.md`, `LIMS_VENDOR_FINDINGS.md`) הועברו **ללא שינוי תוכן מהותי** — רק עדכון הפניות-מיקום (בית קנוני, PROCESS.md). היסטוריית הבנייה המקורית (כולל אימוץ הפרויקט המקביל מ-ChatGPT ב-2026-08-03) מתועדת ב-`water-knowledge-system/PROCESS.md` דרישות #14–#16 ואינה משוכפלת כאן — זהו הפניה, לא עותק.
- **ASSUMPTION**: אין — זו מיגרציה מכנית (מיקום בלבד), לא ייבוא תוכן חדש הטעון בדיקה.
- **DECISION**: המשתמש פתח ריפו ייעודי (`haimk-max/LIMS`) והורה על ניהול המיגרציה אליו (2026-08-11). הריפו-המקור עודכן באותו סבב כך שממשיך להצביע לכאן ולא מחזיק עותק כפול (עקרון "עובדה אחת — בית אחד"). מספור הדרישות ב-PROCESS.md כאן **מתחיל מ-#1** — SSOT עצמאי, לא המשך #17.
- **OPEN**: זהות ספק ה-LIMS הקיים והערוץ אליו; 12 נקודות ההכרעה (D-01…D-12) בחוברת העבודה; גורל שכבת העיבוד אחרי בחינת הספק. פירוט מלא — `docs/lims-icp/START_HERE_he.md`.

## Project Overview

**כותרת**: בחינת מערכת LIMS למעבדה — אפיון תהליך ICP-OES כפיילוט
**קהל יעד**: צוות מעבדת כימיה למי שתייה וסביבה (רשות המים) + בעלי-עניין בבחינת ספק LIMS
**סטטוס נוכחי**: אפיון הכללים ל-ICP-OES בעבודה (חוברת v0.3, 12 כרטיסי הכרעה פתוחים) — **טרם נסגר** מול צוות המעבדה, וטרם הוצג לספק
**מטרה**: (א) לסגור עם צוות המעבדה אפיון מלא ומוסכם של תהליך הריצה האנליטית ב-ICP-OES (כללי קליטה/בקרה/כיול/דיווח/QA), (ב) להציג את התהליך המאופיין לספק ה-LIMS הקיים כנושא הבחינה הראשון — האם ואיך המערכת תומכת בו, מה כקונפיגורציה ומה כפיתוח. **לא** בחינת-חלופות רחבה בשלב זה.

## Scope

בשלב הנוכחי זהו **פרויקט אפיון/מסמכים** — לא מימוש. הריפו מכיל:
- **מסלול ה-ICP** (`docs/lims-icp/`) — הליבה הפעילה: `START_HERE_he.md` (מסמך העוגן; לקרוא ראשון בכל סשן), חוברת העבודה (`ICP_RULE_ENGINE_WORKBOOK_he.html`), קטלוג הכללים (`ICP_LIMS_RULE_CATALOG_he.csv` — מקור-האמת), מחולל הקטלוג (`tools/build_catalog_section.py`), ומסמכי המקור (`sources/`: SOP-05, אפיון v0.1, ארכיון v0.2, קובץ ייצוא אמיתי מהמכשיר)
- **תשתית תומכת לבחינת הספק** (`docs/LIMS_CAPABILITY_MAP.md`, `docs/LIMS_VENDOR_FINDINGS.md`) — מפת דרישות מדורגת ותרחישי הדגמה גנריים + ממצאי בחינת מערכות (LabWare/LabVantage/SampleManager/STARLIMS ומתמחות סביבתיות); התהליך המאופיין ב-`lims-icp/` הוא ה"מטען" שממלא את תרחישי ההדגמה בתוכן אמיתי
- תיעוד ניהול הפרויקט (CLAUDE.md, PROCESS.md, HANDOVER.md, README.md)

### מה לא לעשות (עד סגירת האפיון ובחירת ספק)
- אין לכתוב קוד מימוש (Python/SQL/JS מעבר לכלי-הרינדור הקיים) ללא בקשה מפורשת.
- אין לשנות את תוכן קטלוג הכללים ידנית ב-HTML — כל שינוי כלל דרך ה-CSV, ואז `tools/build_catalog_section.py`.
- אין "לסגור" נקודת הכרעה פתוחה (D-01…D-12) מבלי שהמשתמש/צוות המעבדה מסרו החלטה מפורשת; ניחוש מקצועי אסור (§3 בסדר הסמכות ב-START_HERE_he.md: SOP > נוהל מאושר > החלטת עבודה > נוהג בפועל > הצעה).
- אין להוסיף תלויות, frameworks, או מבני תיקיות ספקולטיביים לקראת מימוש עתידי.

## טבלת תפקידי מסמכים

| מסמך | השאלה שהוא עונה עליה |
|------|----------------------|
| **CLAUDE.md** | מהם הכללים הקבועים לעבודה בפרויקט? |
| **PROCESS.md** | מה פתוח ומה נסגר בריפו הזה (hash + אימות)? |
| **HANDOVER.md** | מה הסשן האחרון הותיר שהבא צריך לדעת? |
| **README.md** | אילו מסמכים קיימים ומה מעמדם (אינדקס)? |
| **docs/lims-icp/START_HERE_he.md** | מצב מסלול ה-ICP, החלטות, חסמים והמשך — לקרוא ראשון בכל סשן על מסלול זה |

## Contact & Escalation

- הכרעות מקצועיות/מעבדתיות (כללי SOP, ספים, מבנה בקרות) → **User decision** (ask, don't assume) — לא לנחש; ראו סדר הסמכות ב-`START_HERE_he.md` §3.
- סתירה בין החלטת-עבודה ל-SOP → אין לתכנת/לאכוף עד יישוב ותיעוד במקור מבוקר.
- שאלות על תהליך/ניהול הריפו → להכריע לפי הכללים כאן; אם לא ברור — לשאול.
